# Backend chưa được deploy. Khi có URL backend thật (vd Render), bỏ comment
# dòng dưới và thay bằng URL đó, rồi build lại frontend.
#   VITE_API_BASE=https://your-backend.onrender.com
#
# Khi để trống/không set: production build dùng đường dẫn tương đối ('') và app
# chạy hoàn toàn bằng dữ liệu local fallback (offline). KHÔNG trỏ về
# poseidonz227.id.vn — host đó phục vụ SPA (trả HTML), không phải API.
