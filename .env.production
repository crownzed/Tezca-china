# Backend deploy trên Fly.io (app "tezca-china", region sin).
# Frontend (Vercel/Firebase) nằm khác domain nên KHÔNG dùng đường dẫn tương đối
# được — phải trỏ thẳng vào URL Fly thì /api/* mới gọi đúng API.
VITE_API_BASE=https://tezca-china.fly.dev
