# Backend deploy trên Render (Blueprint từ render.yaml, service "tiengtrung-api").
# Frontend (Vercel/Firebase) nằm khác domain nên KHÔNG dùng đường dẫn tương đối
# được — phải trỏ thẳng vào URL Render thì /api/* mới gọi đúng API.
VITE_API_BASE=https://tiengtrung-api.onrender.com
